Activate power "kiro-config" if needed. Verify steering files are loaded (check steeringFiles array in activation response). If not loaded, use action="readSteering". DO NOT create steering files - they already exist in the power.

We want to implement a new feature. The description will be at the end of the prompt.

#core-project-system

Follow the plan workflow, running in plan mode.

This prompt is for features with KNOWN implementation approaches. If you need to research use research-feature.md instead.